// Build-time constant injected by Vite (see vite.config.ts `define`).
// Reading from `package.json` at build time keeps the displayed version in
// lockstep with `npm version` bumps without an extra sync step.
declare const __APP_VERSION__: string;
